#ifndef ONLINE_JUDGE
#include <Akshat.hpp>
#else
#define dbg(...) 42
#endif
#include "bits/stdc++.h"
using namespace std;
using ll = int64_t;

void Solution() {
    ll n;
    cin >> n;
    // joining consecutive characters
    // starts and ends with deg 1 characters
    // think about components
}

int main() {
    cin.tie(nullptr)->sync_with_stdio(false);
#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif
    Solution();
    cerr << fixed << setprecision(4) << (double)clock() / CLOCKS_PER_SEC << " secs" << endl;
    return 0;
}