# Why Lazy Segment Trees?
We can do commutative operations like $sum$, $gcd$, etc. using a normal segment tree. But if we need to do range updates using non-commutative operations we need another technique.
